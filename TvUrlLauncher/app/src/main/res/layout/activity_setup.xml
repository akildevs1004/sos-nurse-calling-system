<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <!-- MAIN CONTENT (already centered) -->
    <LinearLayout
        android:id="@+id/contentLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center"
        android:padding="24dp">

        <!-- LOGIN IMAGE -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:src="@drawable/login_image"
            android:scaleType="fitCenter"
            android:layout_marginBottom="24dp" />

            <RadioGroup
    android:id="@+id/protocolGroup"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:layout_gravity="center_horizontal"
    android:layout_marginBottom="16dp">

    <RadioButton
        android:id="@+id/rbHttp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="HTTP"
        android:checked="true"
        android:padding="10dp" />

    <RadioButton
        android:id="@+id/rbHttps"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="HTTPS"
        android:padding="10dp" />
</RadioGroup>


        <EditText
            android:id="@+id/ipInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="IP Address/ Domain Name"
            android:background="@drawable/focus_edittext"
            android:padding="14dp"
            android:singleLine="true"/>

        <EditText
            android:id="@+id/portInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Port (optional)"
            android:background="@drawable/focus_edittext"
            android:padding="14dp"
            android:layout_marginTop="12dp"
            android:singleLine="true"/>

        <EditText
            android:id="@+id/pathInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Path (optional)"
            android:background="@drawable/focus_edittext"
            android:padding="14dp"
            android:layout_marginTop="12dp"
            android:singleLine="true"/>

        <Button
            android:id="@+id/saveOpenBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="SAVE &amp; OPEN"
            android:textSize="18sp"
            android:padding="16dp"
            android:layout_marginTop="20dp"
            android:backgroundTint="#6A00FF"
            android:textColor="@android:color/white"/>

    </LinearLayout>

    <!-- 🔄 LOADING OVERLAY -->
    <FrameLayout
        android:id="@+id/loadingOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#80FFFFFF"
        android:visibility="gone">

        <ProgressBar
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:indeterminate="true" />

    </FrameLayout>

</FrameLayout>
